<%= form_with(model: chatroom, local: true) do |form| %>
  <% if chatroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chatroom.errors.count, "error") %> prohibited this chatroom from being saved:</h2>

      <ul>
      <% chatroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control", style: "margin-bottom: 15px" %>
  </div>

  <div class="field">
    <%= form.label :room_size %>
    <%= form.select :room_size, 2...11, class: "form-control", style: "margin-bottom: 15px" %>
  </div>

  <div class="actions">
    <%= form.hidden_field :owner_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
